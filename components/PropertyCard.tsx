import Link from "next/link";
import { Bath, BedDouble, CarFront, LandPlot, MoveUpRight, Ruler } from "lucide-react";
import type { Property } from "@/lib/properties";
export function PropertyCard({ property }: { property: Property }) {
  const whatsapp = `https://wa.me/?text=${encodeURIComponent(`Hola Raíz Noble, quiero información sobre: ${property.title}.`)}`;
  return <article className="property-card"><Link href={`/propiedades/${property.slug}`} className="property-image"><img src={property.image} alt={property.title} /><span className={`property-tag tag-${property.operation.toLowerCase().replace("ó", "o")}`}>{property.operation}</span></Link><div className="property-content"><div className="property-price">{property.priceLabel}</div><div className="property-location">{property.location}</div><h3><Link href={`/propiedades/${property.slug}`}>{property.title}</Link></h3><div className="property-stats">{property.land > 0 && <span><LandPlot /> {property.land} m² terreno</span>}{property.construction > 0 && <span><Ruler /> {property.construction} m² const.</span>}{property.bedrooms > 0 && <span><BedDouble /> {property.bedrooms}</span>}{property.bathrooms > 0 && <span><Bath /> {property.bathrooms}</span>}{property.parking > 0 && <span><CarFront /> {property.parking}</span>}</div><div className="property-actions"><Link className="button ink small" href={`/propiedades/${property.slug}`}>Ver propiedad <MoveUpRight size={16} /></Link><a className="button outline small" href={whatsapp} target="_blank" rel="noreferrer">Preguntar por WhatsApp</a></div></div></article>;
}
